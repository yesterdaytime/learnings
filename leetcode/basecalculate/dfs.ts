export class Calculate {
    /**
     *
     * 1. 开始位置，给path = []
     * 2. 判断方向， 遇到0，不能通过，遇到1，可以走
     * 3.
     *
     *
     */

    books: boolean[][] = [];
    directs = [];

    dfs(x: number, y: number, books: boolean[][]) {
        if (this.books[x][y]) {
        }
    }
}
